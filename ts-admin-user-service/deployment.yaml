apiVersion: v1
kind: Service
metadata:
  name: ts-admin-user-service
  labels:
    app: ts-admin-user-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-admin-user-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-admin-user-service
  labels:
    app: ts-admin-user-service
spec:
  selector:
    matchLabels:
      app: ts-admin-user-service
  template:
    metadata:
      labels:
        app: ts-admin-user-service
    spec:
      containers:
      - name: ts-admin-user-service
        image: 192.168.1.105/library/ts-admin-user-service
        envFrom:
          - secretRef:
              name: ts-admin-user-service
