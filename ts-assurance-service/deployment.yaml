apiVersion: v1
kind: Service
metadata:
  name: ts-assurance-service
  labels:
    app: ts-assurance-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-assurance-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-assurance-service
  labels:
    app: ts-assurance-service
spec:
  selector:
    matchLabels:
      app: ts-assurance-service
  template:
    metadata:
      labels:
        app: ts-assurance-service
    spec:
      containers:
      - name: ts-assurance-service
        image: 192.168.1.105/library/ts-assurance-service
        envFrom:
          - secretRef:
              name: ts-assurance-service
