apiVersion: v1
kind: Service
metadata:
  name: ts-auth-service
  labels:
    app: ts-auth-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-auth-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-auth-service
  labels:
    app: ts-auth-service
spec:
  selector:
    matchLabels:
      app: ts-auth-service
  template:
    metadata:
      labels:
        app: ts-auth-service
    spec:
      containers:
      - name: ts-auth-service
        image: 192.168.1.105/library/ts-auth-service
        envFrom:
          - secretRef:
              name: ts-auth-service
