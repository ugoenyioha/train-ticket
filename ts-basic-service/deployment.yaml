apiVersion: v1
kind: Service
metadata:
  name: ts-basic-service
  labels:
    app: ts-basic-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-basic-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-basic-service
  labels:
    app: ts-basic-service
spec:
  selector:
    matchLabels:
      app: ts-basic-service
  template:
    metadata:
      labels:
        app: ts-basic-service
    spec:
      containers:
      - name: ts-basic-service
        image: 192.168.1.105/library/ts-basic-service
        envFrom:
          - secretRef:
              name: ts-basic-service
