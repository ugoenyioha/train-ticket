apiVersion: v1
kind: Service
metadata:
  name: ts-cancel-service
  labels:
    app: ts-cancel-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-cancel-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-cancel-service
  labels:
    app: ts-cancel-service
spec:
  selector:
    matchLabels:
      app: ts-cancel-service
  template:
    metadata:
      labels:
        app: ts-cancel-service
    spec:
      containers:
      - name: ts-cancel-service
        image: 192.168.1.105/library/ts-cancel-service
        envFrom:
          - secretRef:
              name: ts-cancel-service
