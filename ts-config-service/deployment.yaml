apiVersion: v1
kind: Service
metadata:
  name: ts-config-service
  labels:
    app: ts-config-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-config-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-config-service
  labels:
    app: ts-config-service
spec:
  selector:
    matchLabels:
      app: ts-config-service
  template:
    metadata:
      labels:
        app: ts-config-service
    spec:
      containers:
      - name: ts-config-service
        image: 192.168.1.105/library/ts-config-service
        envFrom:
          - secretRef:
              name: ts-config-service
