apiVersion: v1
kind: Service
metadata:
  name: ts-consign-price-service
  labels:
    app: ts-consign-price-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-consign-price-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-consign-price-service
  labels:
    app: ts-consign-price-service
spec:
  selector:
    matchLabels:
      app: ts-consign-price-service
  template:
    metadata:
      labels:
        app: ts-consign-price-service
    spec:
      containers:
      - name: ts-consign-price-service
        image: 192.168.1.105/library/ts-consign-price-service
        envFrom:
          - secretRef:
              name: ts-consign-price-service
