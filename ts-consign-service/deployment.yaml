apiVersion: v1
kind: Service
metadata:
  name: ts-consign-service
  labels:
    app: ts-consign-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-consign-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-consign-service
  labels:
    app: ts-consign-service
spec:
  selector:
    matchLabels:
      app: ts-consign-service
  template:
    metadata:
      labels:
        app: ts-consign-service
    spec:
      containers:
      - name: ts-consign-service
        image: 192.168.1.105/library/ts-consign-service
        envFrom:
          - secretRef:
              name: ts-consign-service
