apiVersion: v1
kind: Service
metadata:
  name: ts-contacts-service
  labels:
    app: ts-contacts-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-contacts-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-contacts-service
  labels:
    app: ts-contacts-service
spec:
  selector:
    matchLabels:
      app: ts-contacts-service
  template:
    metadata:
      labels:
        app: ts-contacts-service
    spec:
      containers:
      - name: ts-contacts-service
        image: 192.168.1.105/library/ts-contacts-service
        envFrom:
          - secretRef:
              name: ts-contacts-service
