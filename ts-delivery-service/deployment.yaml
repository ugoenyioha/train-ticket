apiVersion: v1
kind: Service
metadata:
  name: ts-delivery-service
  labels:
    app: ts-delivery-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-delivery-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-delivery-service
  labels:
    app: ts-delivery-service
spec:
  selector:
    matchLabels:
      app: ts-delivery-service
  template:
    metadata:
      labels:
        app: ts-delivery-service
    spec:
      containers:
      - name: ts-delivery-service
        image: 192.168.1.105/library/ts-delivery-service
        envFrom:
          - secretRef:
              name: ts-delivery-service
