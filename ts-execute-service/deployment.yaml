apiVersion: v1
kind: Service
metadata:
  name: ts-execute-service
  labels:
    app: ts-execute-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-execute-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-execute-service
  labels:
    app: ts-execute-service
spec:
  selector:
    matchLabels:
      app: ts-execute-service
  template:
    metadata:
      labels:
        app: ts-execute-service
    spec:
      containers:
      - name: ts-execute-service
        image: 192.168.1.105/library/ts-execute-service
        envFrom:
          - secretRef:
              name: ts-execute-service
