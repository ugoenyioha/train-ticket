apiVersion: v1
kind: Service
metadata:
  name: ts-gateway-service
  labels:
    app: ts-gateway-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-gateway-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-gateway-service
  labels:
    app: ts-gateway-service
spec:
  selector:
    matchLabels:
      app: ts-gateway-service
  template:
    metadata:
      labels:
        app: ts-gateway-service
    spec:
      containers:
      - name: ts-gateway-service
        image: 192.168.1.105/library/ts-gateway-service
        envFrom:
          - secretRef:
              name: ts-gateway-service
