apiVersion: v1
kind: Service
metadata:
  name: ts-inside-payment-service
  labels:
    app: ts-inside-payment-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-inside-payment-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-inside-payment-service
  labels:
    app: ts-inside-payment-service
spec:
  selector:
    matchLabels:
      app: ts-inside-payment-service
  template:
    metadata:
      labels:
        app: ts-inside-payment-service
    spec:
      containers:
      - name: ts-inside-payment-service
        image: 192.168.1.105/library/ts-inside-payment-service
        envFrom:
          - secretRef:
              name: ts-inside-payment-service
