apiVersion: v1
kind: Service
metadata:
  name: ts-notification-service
  labels:
    app: ts-notification-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-notification-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-notification-service
  labels:
    app: ts-notification-service
spec:
  selector:
    matchLabels:
      app: ts-notification-service
  template:
    metadata:
      labels:
        app: ts-notification-service
    spec:
      containers:
      - name: ts-notification-service
        image: 192.168.1.105/library/ts-notification-service
        envFrom:
          - secretRef:
              name: ts-notification-service
