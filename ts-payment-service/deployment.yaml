apiVersion: v1
kind: Service
metadata:
  name: ts-payment-service
  labels:
    app: ts-payment-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-payment-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-payment-service
  labels:
    app: ts-payment-service
spec:
  selector:
    matchLabels:
      app: ts-payment-service
  template:
    metadata:
      labels:
        app: ts-payment-service
    spec:
      containers:
      - name: ts-payment-service
        image: 192.168.1.105/library/ts-payment-service
        envFrom:
          - secretRef:
              name: ts-payment-service
