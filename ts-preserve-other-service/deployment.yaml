apiVersion: v1
kind: Service
metadata:
  name: ts-preserve-other-service
  labels:
    app: ts-preserve-other-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-preserve-other-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-preserve-other-service
  labels:
    app: ts-preserve-other-service
spec:
  selector:
    matchLabels:
      app: ts-preserve-other-service
  template:
    metadata:
      labels:
        app: ts-preserve-other-service
    spec:
      containers:
      - name: ts-preserve-other-service
        image: 192.168.1.105/library/ts-preserve-other-service
        envFrom:
          - secretRef:
              name: ts-preserve-other-service
