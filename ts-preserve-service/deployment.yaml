apiVersion: v1
kind: Service
metadata:
  name: ts-preserve-service
  labels:
    app: ts-preserve-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-preserve-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-preserve-service
  labels:
    app: ts-preserve-service
spec:
  selector:
    matchLabels:
      app: ts-preserve-service
  template:
    metadata:
      labels:
        app: ts-preserve-service
    spec:
      containers:
      - name: ts-preserve-service
        image: 192.168.1.105/library/ts-preserve-service
        envFrom:
          - secretRef:
              name: ts-preserve-service
