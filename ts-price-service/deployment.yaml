apiVersion: v1
kind: Service
metadata:
  name: ts-price-service
  labels:
    app: ts-price-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-price-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-price-service
  labels:
    app: ts-price-service
spec:
  selector:
    matchLabels:
      app: ts-price-service
  template:
    metadata:
      labels:
        app: ts-price-service
    spec:
      containers:
      - name: ts-price-service
        image: 192.168.1.105/library/ts-price-service
        envFrom:
          - secretRef:
              name: ts-price-service
