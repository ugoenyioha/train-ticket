apiVersion: v1
kind: Service
metadata:
  name: ts-rebook-service
  labels:
    app: ts-rebook-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-rebook-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-rebook-service
  labels:
    app: ts-rebook-service
spec:
  selector:
    matchLabels:
      app: ts-rebook-service
  template:
    metadata:
      labels:
        app: ts-rebook-service
    spec:
      containers:
      - name: ts-rebook-service
        image: 192.168.1.105/library/ts-rebook-service
        envFrom:
          - secretRef:
              name: ts-rebook-service
