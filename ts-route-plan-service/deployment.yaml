apiVersion: v1
kind: Service
metadata:
  name: ts-route-plan-service
  labels:
    app: ts-route-plan-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-route-plan-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-route-plan-service
  labels:
    app: ts-route-plan-service
spec:
  selector:
    matchLabels:
      app: ts-route-plan-service
  template:
    metadata:
      labels:
        app: ts-route-plan-service
    spec:
      containers:
      - name: ts-route-plan-service
        image: 192.168.1.105/library/ts-route-plan-service
        envFrom:
          - secretRef:
              name: ts-route-plan-service
