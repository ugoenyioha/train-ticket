apiVersion: v1
kind: Service
metadata:
  name: ts-route-service
  labels:
    app: ts-route-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-route-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-route-service
  labels:
    app: ts-route-service
spec:
  selector:
    matchLabels:
      app: ts-route-service
  template:
    metadata:
      labels:
        app: ts-route-service
    spec:
      containers:
      - name: ts-route-service
        image: 192.168.1.105/library/ts-route-service
        envFrom:
          - secretRef:
              name: ts-route-service
