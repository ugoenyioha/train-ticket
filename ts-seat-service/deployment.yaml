apiVersion: v1
kind: Service
metadata:
  name: ts-seat-service
  labels:
    app: ts-seat-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-seat-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-seat-service
  labels:
    app: ts-seat-service
spec:
  selector:
    matchLabels:
      app: ts-seat-service
  template:
    metadata:
      labels:
        app: ts-seat-service
    spec:
      containers:
      - name: ts-seat-service
        image: 192.168.1.105/library/ts-seat-service
        envFrom:
          - secretRef:
              name: ts-seat-service
