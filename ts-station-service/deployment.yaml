apiVersion: v1
kind: Service
metadata:
  name: ts-station-service
  labels:
    app: ts-station-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-station-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-station-service
  labels:
    app: ts-station-service
spec:
  selector:
    matchLabels:
      app: ts-station-service
  template:
    metadata:
      labels:
        app: ts-station-service
    spec:
      containers:
      - name: ts-station-service
        image: 192.168.1.105/library/ts-station-service
        envFrom:
          - secretRef:
              name: ts-station-service
