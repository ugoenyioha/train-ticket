apiVersion: v1
kind: Service
metadata:
  name: ts-ticket-office-service
  labels:
    app: ts-ticket-office-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-ticket-office-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-ticket-office-service
  labels:
    app: ts-ticket-office-service
spec:
  selector:
    matchLabels:
      app: ts-ticket-office-service
  template:
    metadata:
      labels:
        app: ts-ticket-office-service
    spec:
      containers:
      - name: ts-ticket-office-service
        image: 192.168.1.105/library/ts-ticket-office-service
        envFrom:
          - secretRef:
              name: ts-ticket-office-service
