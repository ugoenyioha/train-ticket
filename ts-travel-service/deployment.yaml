apiVersion: v1
kind: Service
metadata:
  name: ts-travel-service
  labels:
    app: ts-travel-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-travel-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-travel-service
  labels:
    app: ts-travel-service
spec:
  selector:
    matchLabels:
      app: ts-travel-service
  template:
    metadata:
      labels:
        app: ts-travel-service
    spec:
      containers:
      - name: ts-travel-service
        image: 192.168.1.105/library/ts-travel-service
        envFrom:
          - secretRef:
              name: ts-travel-service
