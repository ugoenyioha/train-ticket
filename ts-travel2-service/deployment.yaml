apiVersion: v1
kind: Service
metadata:
  name: ts-travel2-service
  labels:
    app: ts-travel2-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-travel2-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-travel2-service
  labels:
    app: ts-travel2-service
spec:
  selector:
    matchLabels:
      app: ts-travel2-service
  template:
    metadata:
      labels:
        app: ts-travel2-service
    spec:
      containers:
      - name: ts-travel2-service
        image: 192.168.1.105/library/ts-travel2-service
        envFrom:
          - secretRef:
              name: ts-travel2-service
