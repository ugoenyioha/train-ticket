apiVersion: v1
kind: Service
metadata:
  name: ts-user-service
  labels:
    app: ts-user-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-user-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-user-service
  labels:
    app: ts-user-service
spec:
  selector:
    matchLabels:
      app: ts-user-service
  template:
    metadata:
      labels:
        app: ts-user-service
    spec:
      containers:
      - name: ts-user-service
        image: 192.168.1.105/library/ts-user-service
        envFrom:
          - secretRef:
              name: ts-user-service
