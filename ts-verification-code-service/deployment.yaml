apiVersion: v1
kind: Service
metadata:
  name: ts-verification-code-service
  labels:
    app: ts-verification-code-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-verification-code-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-verification-code-service
  labels:
    app: ts-verification-code-service
spec:
  selector:
    matchLabels:
      app: ts-verification-code-service
  template:
    metadata:
      labels:
        app: ts-verification-code-service
    spec:
      containers:
      - name: ts-verification-code-service
        image: 192.168.1.105/library/ts-verification-code-service
        envFrom:
          - secretRef:
              name: ts-verification-code-service
