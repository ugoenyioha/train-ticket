apiVersion: v1
kind: Service
metadata:
  name: ts-voucher-service
  labels:
    app: ts-voucher-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  clusterIP: None
  selector:
    app: ts-voucher-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-voucher-service
  labels:
    app: ts-voucher-service
spec:
  selector:
    matchLabels:
      app: ts-voucher-service
  template:
    metadata:
      labels:
        app: ts-voucher-service
    spec:
      containers:
      - name: ts-voucher-service
        image: 192.168.1.105/library/ts-voucher-service
        envFrom:
          - secretRef:
              name: ts-voucher-service
